#include <cmath>
#include <iostream>
#include <map>
#include <numeric>
#include <regex>
#include <string>
#include <vector>

#define watch(x) std::cout << (#x) << " is " << (x) << '\n'

int main() {
  int x = 20;
  watch(x);

  return 0;
}
